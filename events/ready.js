const { connection } = require('../utils/database');
module.exports = async (client) => {
    console.log(`Started Up!`);
    /*try {
        connection.sync();
        console.log('Synced!')
    } catch(e) {
        console.log(e);
    }*/
    //status loop?
};